# مشخصات فنی و رفتاری Wumpus World

**وضعیت:** v1.0 Frozen — آمادهٔ پیاده‌سازی  
**دامنه:** نسخهٔ تک‌عامل، گرید ثابت ۸×۸، سه حل‌کننده

## 1. هدف و حدود

سیستم باید یک نقشه و پارامترهای بازی را بخواند، قوانین Wumpus World تعریف‌شده توسط استاد را با یک موتور مشترک اجرا کند، سه عامل متفاوت را روی آن اجرا کند و خروجی قابل‌مقایسه تولید کند.

### داخل دامنه

- parser و validation ورودی؛
- موتور قطعی بازی و تولید ادراک؛
- A\* آفلاین با نقشهٔ کامل؛
- عامل آنلاین قاعده‌محور با مشاهدهٔ ناقص؛
- عامل آنلاین یادگیری نظارت‌شده؛
- تولید داده، آموزش، اجرای batch و گزارش معیارها؛
- CLI، تست و مستندات.

### خارج از دامنهٔ نسخهٔ پایه

- رابط گرافیکی؛
- بازی دونفره؛
- شلیک تیر به غول؛
- حرکت قطری؛
- محیط پویا یا جابه‌جایی غول؛
- یادگیری تقویتی یا الگوریتم ژنتیک؛
- سرویس وب و پایگاه داده.

## 2. واژگان و مدل مشاهده

| واژه | تعریف |
| --- | --- |
| Hidden map | آرایش کامل دیوار، چاه، غول و طلا؛ فقط موتور و عامل جست‌وجو می‌بینند |
| Observation | اطلاعات مجاز عامل آنلاین در یک گام |
| Percept | حس محلی شامل breeze، stench و glitter |
| State | وضعیت کامل بازی شامل موقعیت، جان، طلاهای باقی‌مانده و پایان |
| Knowledge state | باورهای عامل از خانه‌های `SAFE`، `PIT?`، `WUMPUS?`، `UNKNOWN` و بازدیدشده |
| Episode | یک اجرای کامل از شروع تا برد، باخت یا توقف ایمنی |

## 3. قالب ورودی

قالب اصلی پیشنهادی:

```text
********
**D*****
*****G**
W***P***
********
********
********
********
100
25
-10
8 8
```

### قواعد parser

1. هشت خط اول دقیقاً هشت نویسه و فقط از مجموعهٔ `{*, P, W, D, G}` دارند.
2. خط ۹: `initial_health`، عدد صحیح مثبت.
3. خط ۱۰: `gold_value`، عدد صحیح نامنفی.
4. خط ۱۱: `pit_score_delta`، عدد صحیح؛ در مدل داخلی باید نامثبت باشد. برای جلوگیری از خطای ورودی، مقدار مثبت با هشدار به `-abs(value)` نرمال می‌شود.
5. خط ۱۲: خروج؛ parser قالب‌های `row column`، `row,column` و `(row,column)` را می‌پذیرد، ولی مستندات فقط `row column` را تولید می‌کنند.
6. مختصات بیرونی یک‌مبنا و به‌شکل `(row, column)` است. اولین خط نقشه `row=1` و چپ‌ترین نویسه `column=1` است.
7. شروع ثابت `(1,1)` است.
8. شروع نباید دیوار، غول یا چاه باشد؛ طلا روی شروع مجاز است و هنگام مقداردهی اولیه مثل ورود به خانه برداشته می‌شود. خروج نباید دیوار یا غول باشد؛ طلا و چاه روی خروج مجازند (DEC-004).
9. خطوط خالی انتهایی مجاز، ولی دادهٔ اضافهٔ غیرخالی خطاست.

> نکته: نمونهٔ ناقص داخل متن رسمی یک سطر کوتاه دارد و سپس `...` نشان می‌دهد؛ آن قطعه صرفاً نمایشی است و ورودی معتبر محسوب نمی‌شود.

## 4. مدل داده

```text
Position(row: int, col: int)
Tile = EMPTY | PIT | WUMPUS | WALL | GOLD
Action = UP | DOWN | LEFT | RIGHT
Status = RUNNING | WON | DEAD_WUMPUS | DEAD_HEALTH | NO_SOLUTION | STEP_LIMIT | AGENT_ERROR | ENGINE_ERROR

GameConfig(
  initial_health,
  gold_value,
  pit_score_delta,
  exit_position,
  grid_size=8,
  pit_rounding="floor_with_min_one",
  max_steps
)

GameState(
  position,
  health,
  collected_gold,
  remaining_gold,
  pit_entries,
  steps,
  status,
  event_log
)
```

نماد `G` پس از برداشت در state به خانهٔ خالی تبدیل می‌شود؛ نقشهٔ پایه immutable می‌ماند و `remaining_gold` مشخص می‌کند کدام طلاها هنوز وجود دارند.

## 5. رفتار موتور

### 5.1 کنش قانونی

- مقصد باید داخل گرید و غیر دیوار باشد.
- عامل‌های استاندارد فقط کنش قانونی می‌دهند.
- اگر عامل کنش غیرقانونی داد، runner آن episode را با خطای عامل ثبت می‌کند؛ موتور بی‌صدا آن را به حرکت دیگری تبدیل نمی‌کند.

### 5.2 ترتیب رویداد یک حرکت

برای state در حال اجرا و action قانونی:

1. موقعیت به مقصد تغییر می‌کند.
2. `steps += 1` و `health -= 1`.
3. اگر `health <= 0`، وضعیت `DEAD_HEALTH` و episode تمام می‌شود؛ بنابراین رسیدن با `health=0` حتی روی خروج برد محسوب نمی‌شود.
4. اگر مقصد غول است، وضعیت `DEAD_WUMPUS` و episode تمام می‌شود.
5. اگر مقصد چاه است، `pit_entries += 1` و `health = max(1, health // 2)`. ورود دوباره به همان چاه، هر بار اثر جان و جریمهٔ جداگانه دارد.
6. اگر مقصد طلای باقی‌مانده است، طلا خودکار برداشته می‌شود.
7. اگر مقصد خروج است و عامل زنده است، وضعیت `WON` می‌شود.
8. در غیر این صورت، observation جدید تولید می‌شود.

این ترتیب **قرارداد قطعی** پروژه است (DEC-004).

### 5.3 ادراک

در موقعیت فعلی:

- `breeze=True` اگر حداقل یک چاه در چهار همسایگی باشد؛
- `stench=True` اگر حداقل یک غول در چهار همسایگی باشد؛
- `glitter=True` اگر طلای برنداشته‌شده روی خود خانه باشد؛
- `at_exit=True` اگر موقعیت فعلی خروج باشد؛
- عامل موقعیت، جان، طلاهای جمع‌شده، تعداد گام و actionهای قانونی را نیز می‌داند.

ادراک، نوع خانه‌های ناشناخته یا مکان خطرها را افشا نمی‌کند.

### 5.4 برد و امتیاز

- برد: رسیدن زنده به خروج؛ برداشتن همهٔ طلاها در قرارداد فعلی اجباری نیست.
- امتیاز نهایی رسمی فقط برای `WON` گزارش می‌شود:

```text
final_score = remaining_health
            + collected_gold * gold_value
            + pit_entries * pit_score_delta
```

چون `pit_score_delta <= 0` است، جملهٔ آخر جریمه است. برای episode ناموفق، `final_score` تهی (`None`) و یک `diagnostic_score` با همین فرمول صرفاً برای تحلیل ذخیره می‌شود تا شکست با برد اشتباه نشود.

### 5.5 پایان ایمنی

- هر episode یک `max_steps` دارد تا عامل در حلقهٔ بی‌نهایت نماند.
- مقدار پیش‌فرض قطعی: `min(initial_health, 4 * grid_size * grid_size)`؛ این مقدار حد طبیعی جان را لحاظ می‌کند و هم safety cap دارد.
- در صورت عبور از حد، وضعیت `STEP_LIMIT` ثبت می‌شود.
- runner خطاها را با scope باریک تفکیک می‌کند: exception از کد عامل (`reset`،
  `choose_action`، `observe_transition`) یا بازگرداندن کنش غیرقانونی → وضعیت
  `AGENT_ERROR`؛ نقص در ساخت observation یا موتور → وضعیت `ENGINE_ERROR`. در هر
  دو حالت episode با متن خطا ثبت شده و اجرای batch ادامه می‌یابد.

## 6. API عامل‌ها

رابط مفهومی مشترک:

```python
class Agent(Protocol):
    def reset(self, config, public_map_info, seed) -> None: ...
    def choose_action(self, observation) -> Action: ...
    def observe_transition(self, observation, action, outcome) -> None: ...
```

- `public_map_info` فقط اندازه، موقعیت شروع و خروج و مقادیر امتیاز را دارد.
- SearchAgent استثنائاً از `FullMapAgent` استفاده می‌کند و نقشهٔ کامل را می‌گیرد؛ این تفاوت در نتایج علامت‌گذاری می‌شود.
- RuleAgent و MLAgent هرگز reference شیء `GameMap` مخفی را دریافت نمی‌کنند.

## 7. مشخصات روش اول: A\* آفلاین

### 7.1 مسئلهٔ جست‌وجو

- state: `(position, health, remaining_gold_mask)`؛
- start: `(1,1)` با جان اولیه و تمام طلاها؛
- successor: هر حرکت قانونی طبق موتور؛ successor مرگ‌آور حذف می‌شود؛
- goal: رسیدن زنده به خروج؛
- نقشه و خطرها کاملاً معلوم‌اند؛ breeze/stench استفاده نمی‌شوند.

### 7.2 تبدیل بیشینه‌سازی امتیاز به کمینه‌سازی هزینه

ثابت زیر را در نظر می‌گیریم:

```text
C = initial_health + total_gold_count * gold_value
```

برای هر مسیر موفق:

```text
loss = (initial_health - remaining_health)
     + pit_entries * abs(pit_score_delta)
     + remaining_gold_count * gold_value

final_score = C - loss
```

پس کمینه‌کردن `loss` معادل بیشینه‌کردن امتیاز است. کاهش واقعی جان و جریمهٔ چاه به transition cost افزوده می‌شود و در خروج، ارزش طلاهای برنداشته‌شده به‌عنوان terminal cost اعمال می‌شود.

### 7.3 heuristic

نسخهٔ مطمئن برای stateهای غیرهدف:

```text
h(n) = ManhattanDistance(current_position, exit)
```

این heuristic حداقل هزینهٔ حرکت تا خروج را تخمین می‌زند و دیوار، چاه و طلای
ازدست‌رفته را نادیده می‌گیرد، بنابراین بیش‌برآورد نمی‌کند. برای state خروج،
هزینهٔ دقیق طلای جمع‌نشده هنگام محاسبهٔ اولویت heap افزوده می‌شود؛ در غیر این
صورت ممکن است A\* یک خروج نزدیک ولی کم‌امتیاز را پیش از مسیر بهتر بپذیرد.

### 7.4 خروجی تشخیصی

مسیر، امتیاز پیش‌بینی‌شده، زمان برنامه‌ریزی، تعداد stateهای گسترش‌یافته، بیشینهٔ frontier و علت نبودن راه‌حل ذخیره می‌شود.

## 8. مشخصات روش دوم: عامل قاعده‌محور

### 8.1 دانش نگهداری‌شده

- `Visited(c)` و observation هر خانهٔ بازدیدشده؛
- `Safe(c)`؛
- `PossiblePit(c)` / `ConfirmedPit(c)`؛
- `PossibleWumpus(c)` / `ConfirmedWumpus(c)`؛
- `NotPit(c)` / `NotWumpus(c)`: فکت‌های منفیِ **دائمی** که از ادراک عدم نسیم/بو
  اثبات می‌شوند و مانع مشکوک‌شدن دوبارهٔ خانه می‌شوند؛
- `KnownPit(c)`: چاهی که عامل واردش شده و آسیب دیده (قابل‌عبور در اضطرار، ولی
  خارج از مسیریابی امنِ عادی)؛
- `Blocked(c)` برای مرز و دیوارهای کشف‌شده؛
- `Frontier(c)` برای خانهٔ دیده‌نشدهٔ مجاور ناحیهٔ شناخته‌شده.

### 8.2 قواعد حداقلی

برای خانهٔ فعلی `c` و هر همسایهٔ معتبر `n`:

- اگر در `c` نسیم نیست، `n` **برای همیشه** چاه نیست (`NotPit`)؛ این اثبات مستقل از منبع است و مانع مشکوک‌شدن دوبارهٔ `n` می‌شود.
- اگر در `c` بوی بد نیست، `n` **برای همیشه** غول نیست (`NotWumpus`).
- اگر `n` نه چاه ممکن/قطعی و نه غول ممکن/قطعی باشد، `n` امن است.
- اگر در `c` نسیم هست، حداقل یکی از همسایه‌های حل‌نشده چاه است؛ همه کاندید می‌شوند، نه قطعی.
- اگر در `c` بوی بد هست، حداقل یکی از همسایه‌های حل‌نشده غول است؛ همه کاندید می‌شوند، نه قطعی.
- اگر پس از حذف خانه‌های ردشده فقط یک کاندید برای یک percept باقی ماند **و** آن percept توسط خطرِ قطعی یا چاهِ ازقبل‌واردشده توضیح داده نشده باشد، خطر آن خانه قطعی می‌شود.
- **محیط چند-خطره:** چون چند چاه و تا دو غول ممکن است، تأیید یک خطر هرگز کاندیداهای دیگرِ همان percept را «امن» نمی‌کند (اجتناب از استنتاج ناسالمِ تک‌خطره).

### 8.3 سیاست کنش

اولویت‌ها:

1. اگر بودجهٔ جان تنگ است، بازگشت اضطراری: اول مسیر کاملاً امن، سپس مسیر «قابل‌نجات» که از چاه‌ها می‌گذرد ولی هرگز از خانهٔ مشکوک‌به‌غول؛
2. حرکت به frontier امنِ بازدیدنشده با بهترین utility تقریبی؛
3. بازگشت امن به خروج اگر هدف امن دیگری وجود ندارد؛
4. فقط اگر بدون ریسک هیچ راهی نیست، رتبه‌بندی خانه‌های مرزیِ غیرِقطعی‌کشنده با امتیاز ریسک (غولِ مشکوک ≫ چاهِ مشکوک ≫ ناشناخته) و انتخاب کم‌ریسک‌ترین با tie-breaker قطعی مبتنی بر seed.

> جمع‌آوری طلا هدفِ فعال نیست: چون طلا هنگام ورود خودکار برداشته می‌شود، `glitter` روی خانهٔ برنداشته هرگز مشاهده نمی‌شود (DEC-004) و عامل آنلاین نمی‌تواند مکان طلای دیده‌نشده را هدف بگیرد.

تمام factهای استفاده‌شده و قاعدهٔ نتیجه‌دهنده در reasoning trace ثبت می‌شوند.

## 9. مشخصات روش سوم: یادگیری نظارت‌شده

### 9.1 تعریف مسئلهٔ ML

- نوع: classification چهارکلاسه برای `UP/DOWN/LEFT/RIGHT`؛
- ورودی: فقط state مشاهده‌پذیر و حافظهٔ ساخته‌شده از observationهای گذشته؛
- label: کنش خبرهٔ A\* روی state واقعی متناظر؛
- inference: امتیاز/احتمال چهار کنش، سپس mask کنش‌های غیرقانونی و انتخاب بیشترین مقدار.

### 9.2 ویژگی‌های نسخهٔ پایه

- جان نرمال‌شده، نسبت گام مصرف‌شده، تعداد طلای جمع‌شده؛
- بردار نسبی خروج؛
- breeze، stench، glitter و at_exit؛
- چهار action قانونی؛
- برای هر ۶۴ خانه ۶ channel تخت‌شده: visited، safe، possible_pit، possible_wumpus، blocked و is_current.

هیچ channel از حقیقت پنهانِ چاه یا غول استفاده نمی‌کند.

### 9.3 تولید نمونه

- نقشه‌ها با seed و قیود اعتبار تولید می‌شوند.
- برای هر state قابل‌مشاهده، خبره با نقشهٔ کامل action بهینه را تعیین می‌کند.
- در نسخهٔ فعلی، نمونه‌ها از trajectory اصلی A* در هر نقشه استخراج می‌شوند؛
  افزایش تنوع stateهای recoverable یک مسیر توسعهٔ آینده است.
- داده با `map_id` به train/validation/test تقسیم می‌شود؛ یک نقشه فقط در یک split است.
- config، seed و feature schema همراه داده ذخیره می‌شود.

### 9.4 مدل‌ها

- baseline: انتخاب کلاس پرتکرار پس از legal mask؛
- مدل توضیح‌پذیر: Decision Tree محدود؛
- مدل اصلی پیشنهادی: Random Forest با وزن‌دهی کلاس‌ها؛
- انتخاب نهایی با validation و معیار macro-F1 به‌همراه rollout win rate انجام می‌شود.

### 9.5 معیارهای ML

- آفلاین: accuracy، macro-F1، per-class recall و confusion matrix؛
- آنلاین: win rate، score، Wumpus death، pit entries و moves روی test maps؛
- نتیجهٔ اصلی، عملکرد آنلاین روی suite مستقل است؛ accuracy تقلید فقط معیار کمکی است.

## 10. تولید نقشه و مجموعهٔ داده

مولد باید پارامترهای تعداد دیوار/چاه/غول/طلا را بپذیرد و این قیود را رعایت کند:

- شروع خالی از دیوار، غول و چاه؛ خروج خالی از دیوار و غول (چاه و طلا روی خروج مجازند)؛
- نمادها overlap ندارند؛
- حداقل یک مسیر هندسی از شروع به خروج بدون عبور از دیوار وجود دارد؛
- برای مجموعهٔ آموزشی اصلی، حداقل یک مسیر موفق با جان داده‌شده وجود دارد؛
- map id از seed و محتوای نقشه ساخته می‌شود؛
- مجموعهٔ تست ثابت و پس از انتخاب مدل دست‌نخورده می‌ماند.

نسخهٔ ثبت‌شدهٔ فعلی از ۱۰۰ نقشهٔ تولیدی متنوع استفاده می‌کند و split
`70/15/15` بر اساس `map_id` است. ارزیابی rollout نهایی روی ۲۰ نقشهٔ holdout
با seedهای مستقل `2000..2019` انجام می‌شود.

## 11. اجرای آزمایش و خروجی

هر ردیف نتیجه حداقل این فیلدها را دارد:

```text
run_id, map_id, split, agent, agent_version, visibility,
seed, status, won, final_score, diagnostic_score,
remaining_health, collected_gold, pit_entries, wumpus_death,
steps, runtime_ms, expanded_nodes, peak_frontier, error
```

### قواعد benchmark

- test mapها و config برای هر سه روش یکسان‌اند.
- اجرای نهایی فعلی از seed مشترک `42` استفاده می‌کند. اجرای چند-seed برای
  برآورد پراکندگی دقیق‌تر، کار توسعه‌ای بعدی است و در گزارش به‌عنوان محدودیت
  ذکر می‌شود.
- timeout و سخت‌افزار/نسخهٔ Python ثبت می‌شوند.
- نتایج خام overwrite نمی‌شوند؛ هر experiment یک شناسه و config دارد.
- جدول اصلی، میانگین و میانه را همراه تعداد نمونه و در صورت امکان فاصلهٔ اطمینان bootstrap گزارش می‌کند.

## 12. CLI پیشنهادی

```bash
python -m wumpus validate --input data/maps/example.txt
python -m wumpus run --agent search --input data/maps/example.txt --seed 42
python -m wumpus run --agent rules --input data/maps/example.txt --seed 42 --trace
python -m wumpus dataset generate --config configs/dataset.yaml
python -m wumpus train --config configs/train.yaml
python -m wumpus benchmark --config configs/benchmark.yaml
```

خروجی پیش‌فرض انسان‌خوان و گزینهٔ `--json` ماشین‌خوان است.

## 13. ساختار پیشنهادی مخزن

```text
wumpus-world/
├── README.md
├── pyproject.toml
├── configs/
├── data/
│   ├── maps/
│   ├── processed/
│   └── README.md
├── docs/
│   ├── PROJECT_CONTEXT.md
│   ├── ROADMAP.md
│   ├── SPEC.md
│   └── TASKBOOK.md
├── src/wumpus/
│   ├── core/
│   │   ├── domain.py
│   │   ├── engine.py
│   │   ├── observation.py
│   │   ├── parser.py
│   │   ├── generator.py
│   │   └── runner.py
│   ├── ai/
│   │   ├── search.py
│   │   ├── knowledge.py
│   │   ├── encoder.py
│   │   ├── dataset.py
│   │   └── ml.py
│   ├── agents/
│   │   ├── base.py
│   │   ├── random_agent.py
│   │   ├── greedy_agent.py
│   │   ├── search_agent.py
│   │   ├── rule_agent.py
│   │   └── ml_agent.py
│   ├── evaluation/
│   │   ├── benchmark.py
│   │   └── suite_generator.py
│   └── cli.py
├── tests/
│   ├── core/
│   ├── ai/
│   └── test_benchmark.py
├── artifacts/models/
└── results/
```

فایل‌های بزرگ تولیدشده باید یا قابل‌بازتولید باشند یا در مستندات روش تهیه‌شان ثبت شود.

## 14. نیازمندی‌های غیرعملکردی

- Python 3.11+؛
- type hint برای APIهای عمومی؛
- `pytest` برای تست‌ها؛
- seed صریح برای هر بخش تصادفی؛
- پیام خطای مشخص شامل خط و علت برای ورودی نامعتبر؛
- عدم وابستگی موتور به scikit-learn؛
- log قابل‌خاموش‌کردن و trace اختیاری؛
- اجرای یک نقشهٔ معمولی بدون مدل ML در زمان تعاملی؛
- README شامل نصب، تست، demo و بازتولید آزمایش.

## 15. معیارهای پذیرش سطح بالا

1. همهٔ قوانین قطعی متن رسمی حداقل یک تست دارند.
2. هیچ‌یک از دو عامل آنلاین در تست دسترسی، hidden map را دریافت نمی‌کند.
3. A\* در مسائل کوچک با oracle ساده نتیجهٔ یکسان دارد.
4. trace قاعده‌محور نشان می‌دهد هر ادعا از کدام observation/rule آمده است.
5. تقاطع `map_id` میان train، validation و test خالی است.
6. مدل ذخیره‌شده در یک process تازه بارگذاری و اجرا می‌شود؛ نبود مدل خطای صریح است.
7. benchmark از خرابی یک agent عبور کرده و خطا را در ردیف همان اجرا ثبت می‌کند.
8. جدول نهایی تفاوت visibility را صریح نشان می‌دهد.
9. پروژه با فرمان‌های مستند از محیط تازه نصب، تست و demo می‌شود.

## 16. موارد باز (Frozen — همه حل‌شده)

- [x] اثر دقیق و گردکردن چاه: `max(1, health // 2)` (DEC-004)
- [x] فرمول نهایی امتیاز: `health + gold×value + pits×delta` (DEC-004)
- [x] شرط جمع‌آوری طلا: اختیاری (DEC-004)
- [x] قرارداد مختصات: `(1,1)` = بالاچپ (DEC-004)
- [x] خروج اتوماتیک با ورود به خانه (DEC-004)
- [x] اندازهٔ نسخهٔ ثبت‌شدهٔ دیتاست: ۱۰۰ نقشه، seed آغازین ۱۰۰ و پنج profile متنوع.
